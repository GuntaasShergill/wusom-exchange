// Filled in after `cdk deploy` using the CDK stack outputs. See README "Wire up the frontend config".
window.WUSOM_CONFIG = {
  apiUrl: "https://REPLACE_ME.execute-api.us-east-2.amazonaws.com/dev",
  cognitoDomain: "https://REPLACE_ME.auth.us-east-2.amazoncognito.com",
  userPoolClientId: "REPLACE_ME",
  redirectUri: "https://REPLACE_ME.cloudfront.net/index.html",
};
